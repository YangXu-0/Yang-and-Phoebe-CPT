def act(self, option_selection):
    if self.act_choices.find(a):
        if self.act_choices[index(a): ].find(b):
            if self.act_choices[index(b): ].find(c):
                self.enemy_attributes[y] = True
            else:
                break
        else:
            break
    else:
        self.act_choices.append(self.enemy_attributes[x])
